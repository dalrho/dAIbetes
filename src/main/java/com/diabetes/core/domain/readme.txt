This package contains the domain which is essentially where the data logic lives.
Feel free to modify this package structure however appropriate since this is just a draft to the real project.

Accoridng to AI:
    In standard software architecture—specifically Domain-Driven Design (DDD)—the domain package is the "heart" of your application.
    It contains the business logic and the rules that define the problem you are solving, independent of the database, the UI, or any external frameworks.